# OvaTrack
follow these steps to initialize, install, testnd start the project:

1) python -m venv venv
2) .\venv\Scripts\Activate.ps1
3) pip install -r requirements.txt
4) python train_model.py
5) python train_cycle_model.py
6) python app.py 
